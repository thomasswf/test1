document.getElementById('year').textContent=new Date().getFullYear();document.querySelectorAll('.career-years').forEach(el=>{const start=Number(el.dataset.startYear||2006),max=Number(el.dataset.maxYears||35),now=new Date().getFullYear();el.textContent=Math.min(Math.max(now-start,0),max)});